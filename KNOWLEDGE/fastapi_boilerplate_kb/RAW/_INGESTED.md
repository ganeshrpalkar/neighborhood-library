# Ingested sources — fastapi_boilerplate_kb

Registry of every source in `RAW/`. One row per file, added on ingest.

| Filename | Date added | Source / origin | One-line summary |
|----------|-----------|-----------------|------------------|

_No sources ingested yet. Drop files into `RAW/` (with the required frontmatter) and say "compile"._
